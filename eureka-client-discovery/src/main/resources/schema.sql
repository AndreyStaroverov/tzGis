CREATE TABLE IF NOT EXISTS objects (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  geometric VARCHAR(255) NOT NULL,
  color VARCHAR(255) NOT NULL,
  CONSTRAINT pk_object PRIMARY KEY (id)
);